@model IEnumerable< SCore.Models.User>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    <a class="btn btn-default" asp-action="Create" asp-controller="Users">
        Create
    </a>
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.LastName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Email)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DateOfRegister)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.LastName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Email)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DateOfRegister)
        </td>
        <td>
            <a class="btn btn-default" asp-action="Edit" asp-controller="Users" asp-route-id="@item.Id">
                Edit
            </a>
            <a class="btn btn-default" asp-action="Details" asp-controller="Users" asp-route-id="@item.Id">
                Details
            </a>
            <a class="btn btn-default" asp-action="Delete" asp-controller="Users" asp-route-id="@item.Id">
                Delete
            </a>
        </td>
    </tr>
    }

</table>
